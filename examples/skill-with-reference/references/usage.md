# Usage Notes

This file demonstrates when supporting material should live outside the root `SKILL.md`.

Use a reference file when the material is:

- too long for the main skill instructions
- only needed sometimes
- better reviewed separately
- a checklist, schema, example set, or detailed workflow

Do not use reference files to hide important safety rules, permissions, or risky behavior.
